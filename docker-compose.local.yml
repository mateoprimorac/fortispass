version: '3.9'

services:
  server:
    ports:
      - "0.0.0.0:${LISTEN_PORT:-8080}:8080"
    networks:
      - internal
      - local_access

  nginx:
    profiles:
      - disabled

networks:
  internal:
    internal: true
  local_access:
    driver: bridge
