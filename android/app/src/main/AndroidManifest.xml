<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:name=".FortispassApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Biokey"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Tells AppCompatDelegate to persist per-app locale across process death (API 33+) -->
        <meta-data
            android:name="android.app.locales.autoStoreLocales"
            android:value="true" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.unlock.QRScanActivity"  android:screenOrientation="portrait" />
        <activity android:name=".ui.setup.SetupActivity"    android:screenOrientation="portrait" />
        <activity android:name=".ui.setup.LoginVaultMenuActivity" android:screenOrientation="portrait" />
        <activity android:name=".ui.setup.RecoveryLoginActivity" android:screenOrientation="portrait" />
        <activity android:name=".ui.setup.JoinVaultActivity" android:screenOrientation="portrait" />
        <activity android:name=".ui.settings.AddDeviceActivity" android:screenOrientation="portrait" />
        <activity android:name=".ui.unlock.PairExtensionActivity" android:screenOrientation="portrait" />
        <activity android:name=".ui.settings.SettingsActivity"    android:screenOrientation="portrait" />

    </application>
</manifest>
